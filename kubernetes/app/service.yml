apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  type: NodePort
  selector:
    app: my-nodejs-programming
  ports:
    - ports:
      protocol: TCP
      port: 4000
      targetPort: 4000
      nodePort: 30000
